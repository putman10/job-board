<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Job Board</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
  </head>
  <body>
    <div class="container">
            <p>TEst</p>
    @foreach (var job in Model)
    {
     <p>Job Title: @job.GetTitle()</p>
     <p>Job Description: @job.GetDescription()</p>  
     <p>Job Contact Name: @job.GetContactName()</p> 
     <p>Job Contact Email: @job.GetContactEmail()</p>  
     <p>Job Contact Phone: @job.GetContactPhone()</p>   
    }                 
    </div>
  </body>
</html>
